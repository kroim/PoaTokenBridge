{
  "name": "@omnibridge/subgraph",
  "version": "1.0.0",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.20.1",
    "@graphprotocol/graph-ts": "^0.20.0"
  },
  "license": "MIT",
  "scripts": {
    "auth": "graph auth https://api.thegraph.com/deploy/ $GRAPH_ACCESS_TOKEN",
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "deploy": "graph deploy kroim/xdai-swissdltbridge --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/"    
  },
  "devDependencies": {
    "mustache": "^4.2.0"
  }
}
