
export const isOverridden = (bridgeDirection, token) => false;

export const getOverriddenToToken = (bridgeDirection, token) => null;

export const getOverriddenMode = (bridgeDirection, token) => null;

export const getOverriddenMediator = (bridgeDirection, token) => null;
